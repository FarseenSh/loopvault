# Pinned upstream clone (MystenLabs/deepbookv3) — reproduced by scripts/run-predict-tests.sh
external/

# Project-local Sui config — HOLDS THE PRIVATE KEYSTORE, must never be committed
.sui/

# Move build artifacts
build/
**/build/

# Node / frontend (added in later gates)
node_modules/
.next/
.env
.env.local

# TypeScript / Next generated (auto-regenerated by the build)
*.tsbuildinfo
next-env.d.ts

# Local Claude Code overrides (personal, not shared)
.claude/settings.local.json

# Internal planning / strategy / context docs — kept local, never published
00-HACKATHON-CONTEXT.md
01-RESEARCH.md
02-PRD.md
03-DELIVERABLE.md
CLAUDE.md

# OS / editor
.DS_Store
